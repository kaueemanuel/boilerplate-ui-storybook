# Boilerplate UI Storybook

## Instalação

Para instalar a biblioteca, use npm ou yarn:

`npm install boilerplate-ui-storybook`

ou

`yarn add boilerplate-ui-storybook`

## Uso

```javascript
import { Button } from "boilerplate-ui-storybook";

<Button />;
```
